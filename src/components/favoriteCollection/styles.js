import { css } from "lit-element";

const styles = css`
	.vm-favorite-collection {
		display: flex;
		flex-wrap: wrap;
		justify-content: center;
		margin-top: 1rem;
	}

	.vm-favorite-collection > * {
		margin: 0.5rem;
	}
`;

export { styles };
